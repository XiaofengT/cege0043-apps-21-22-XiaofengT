function menu1(){
	alert("menu number is 1 and menu is called by:"+arguments.callee.name);
}
function menu12(){
	alert("menu number is 12 and menu is called by:"+arguments.callee.name);
}
function menu13(){
	alert("menu number is 13 and menu is called by:"+arguments.callee.name);
}
function menu15(){
	alert("menu number is 15 and menu is called by:"+arguments.callee.name);
}
function menu16(){
	alert("menu number is 16 and menu is called by:"+arguments.callee.name);
}