function menu1(){
	alert("menu number is 1 and menu is called by:"+arguments.callee.name);
}
function menu3(){
	alert("menu number is 3 and menu is called by:"+arguments.callee.name);
}
function menu4(){
	alert("menu number is 4 and menu is called by:"+arguments.callee.name);
}
function menu7(){
	alert("menu number is 7 and menu is called by:"+arguments.callee.name);
}
function menu9(){
	alert("menu number is 9 and menu is called by:"+arguments.callee.name);
}
function menu10(){
	alert("menu number is 10 and menu is called by:"+arguments.callee.name);
}
function menu12(){
	alert("menu number is 12 and menu is called by:"+arguments.callee.name);
}
function menu13(){
	alert("menu number is 13 and menu is called by:"+arguments.callee.name);
}
function menu15(){
	alert("menu number is 15 and menu is called by:"+arguments.callee.name);
}
function menu16(){
	alert("menu number is 16 and menu is called by:"+arguments.callee.name);
}